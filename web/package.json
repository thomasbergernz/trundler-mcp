{
  "name": "trundler-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local chat-agent web frontend for trundler-mcp (grocery pricing). Not published — imports the parent library in-process.",
  "scripts": {
    "dev": "tsx watch server.ts",
    "start": "tsx server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
