{
  "name": "trundler-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tauri v2 desktop shell for the trundler web app. System webview + Node sidecar — no bundled Chromium.",
  "scripts": {
    "fetch-node": "node scripts/fetch-node.mjs",
    "build-server": "node scripts/build-server.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.4.0",
    "esbuild": "^0.25.0"
  }
}
